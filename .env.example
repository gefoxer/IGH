# SQLite (по умолчанию для разработки)
DATABASE_URL="file:./dev.db"

# Секрет для сессий NextAuth — сгенерируйте: openssl rand -base64 32
AUTH_SECRET="замените-на-случайную-длинную-строку"

# URL приложения
AUTH_URL="http://localhost:3000"
